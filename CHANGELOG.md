# Changelog

## Unreleased

- removed the MCP service layer and converted the repository into a direct-script skill package
- added root-level auth setup, docs lookup, HTTP execution, install verification, and smoke-test scripts
- standardized repository-root command execution across Codex and Claude skill entrypoints
- enforced Node.js 18+ as a runtime requirement
- added MIT licensing and open-source release metadata
